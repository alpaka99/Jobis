<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="5dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_chat_name"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12dp"
        android:textStyle="bold"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_chat_msg"
        app:layout_constraintTop_toBottomOf="@id/tv_chat_name"
        app:layout_constraintLeft_toLeftOf="@id/tv_chat_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="300dp"
        android:paddingVertical="4dp"
        android:paddingHorizontal="8dp"
        android:layout_marginTop="2dp"
        android:background="@drawable/view_other_chat_border"
        android:textSize="16dp"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_chat_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/tv_chat_msg"
        app:layout_constraintBottom_toBottomOf="@id/tv_chat_msg"
        android:paddingHorizontal="2dp"
        android:textSize="12dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>